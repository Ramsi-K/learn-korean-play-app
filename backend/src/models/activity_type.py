from enum import Enum


class ActivityType(Enum):
    FLASHCARD = "flashcard"
    SENTENCE_PRACTICE = "sentence_practice"
    GAME_MUNCHER = "game_muncher"
    # Additional types can be added incrementally
    # LISTENING = "listening"
    # WRITING = "writing"
    # SPEAKING = "speaking"
    # etc.
